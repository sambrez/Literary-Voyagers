<div class="text-center">
  <h2>{{name}}'s Books</h2>
  {{#if books.length}}
  <div>
    {{#each books as |book|}}
    {{> allBooks book}}
    {{/each}}
  </div>
  {{else}}
  <p>You don't have any posted book reviews yet.</p>
  <p> Click the button below to get started!</p>
  {{/if}}
  <div>
    <button type="submit"><a href="/api/posts/">Add New Book</a></button>
    </div>
</div>