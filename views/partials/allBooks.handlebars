<div>
  <div>
    <a href="/books/{{id}}">
      <h2>{{title}}</h2><a />
      <h3>Author: {{author}}</h3>
      <h3>Genre: {{genre}}</h3>
      <h3>Recommendation:
        {{#if recommendation}}
        {{get_thumbsUp}}
        {{else}}
        {{get_thumbsDown}}
        {{/if}}
      </h3>
      <h3>Review: {{review}}</h3>
      <h5>Posted by {{user.name}}</h5>
  </div>
</div>